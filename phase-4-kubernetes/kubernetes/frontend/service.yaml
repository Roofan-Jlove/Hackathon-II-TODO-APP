apiVersion: v1
kind: Service
metadata:
  name: todo-frontend
  namespace: todoapp
  labels:
    app: todo-frontend
    tier: frontend
spec:
  type: NodePort
  selector:
    app: todo-frontend
    tier: frontend
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
